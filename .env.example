# Discord Configuration
DISCORD_TOKEN=your_discord_bot_token_here
DISCORD_GUILD_ID=your_guild_id_here

# Wrapper Service Configuration
WRAPPER_URL=http://localhost:8000
WRAPPER_HOST=0.0.0.0
WRAPPER_PORT=8000

# Service Mode:
# - "local": Execute tasks directly on this machine (for user's local wrapper)
# - "orchestrator": Route tasks to registered user wrappers (for central server)
SERVICE_MODE=local

# Security: Allowed directories for project registration
# Comma-separated list of base directories where projects can be registered
# Projects must be within one of these directories (or subdirectories)
# Leave empty for no restrictions (suitable for local development)
# Example: "/home/user/projects,/var/www"
ALLOWED_PROJECT_DIRS=

# Claude Configuration
# NOTE: ANTHROPIC_API_KEY is NOT required for the wrapper.
# The wrapper invokes the Claude Code CLI, which uses its own authentication.
# Run `claude login` to authenticate the CLI before starting the wrapper.
# ANTHROPIC_API_KEY=  # Only needed if using direct API calls (not CLI)

# Phase 2+ (Docker/K8s)
NATS_URL=nats://localhost:4222
REDIS_URL=redis://localhost:6379

# Logging
LOG_LEVEL=info
RUST_LOG=info
